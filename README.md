# Deepread

An elegant, immersive reading environment focused on building a book through deep thinking rather than just consuming content.

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Copy `.env.example` to `.env.local` and set your `MIMO_API_KEY`
3. Run the app:
   `npm run dev`
